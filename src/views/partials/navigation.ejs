<nav class="bg-green-700 text-white px-6 py-4 mb-8">
    <div class="flex justify-between items-center">
        <h1 class="text-2xl font-bold">🦅 BirdWatch</h1>
        <% if (user) { %>
            <a href="/profile" class="text-white">
                <%= user.userName %>
            </a>
            <% } else { %>
                <a href="/login" class="text-white">Přihlásit se</a>
                <% } %>
    </div>
</nav>